# Install PyTorch with CUDA FIRST (required for GPU fine-tuning on Windows):
#   pip install torch==2.11.0 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# Then install the rest:
#   pip install -r requirements.txt
#   pip install -e .

PySide6>=6.6
psutil
nvidia-ml-py
transformers>=4.45
trl>=0.12
peft
bitsandbytes
datasets
accelerate
lighteval
emoji
nltk
huggingface_hub
pyinstaller>=6.0
